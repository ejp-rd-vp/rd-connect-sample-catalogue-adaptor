type: "vapor"
swift_version: "5.0.0"
run_parameters: "serve --port 8080 --hostname 0.0.0.0"

config:
    all:
            catalogURL: "https://samples.rd-connect.eu/"
            localURL: "https://rd-connect.vapor.cloud/"
            
